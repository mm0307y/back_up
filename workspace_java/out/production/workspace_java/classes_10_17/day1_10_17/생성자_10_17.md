## 생성자
- 자바에서 멤버변수의 초기화를 생략할 수 있는건 생성자가 있어서다.
- 디폴트 생성자는 생략할 수 있다.
- 파라미터를 갖는 생성자가 하나라도 있으면 디폴트 생성자는 제공되지 않는다.
- 같은 이름의 생성자를 여러개 선언할 수 있다.
- 생성자 안엥서 메서드를 호출할 수 있다.
- 생성자는 반환타입이 없다. (만일 반환타입을 적으면 메서드 취급을 받으니 주의해야 한다.)
- 생성자 안에서 초기화 작업을 해 볼 수 있다. (static블록을 사용하는 것 보다는 생성자를 사용하는 것이 낫다.) 